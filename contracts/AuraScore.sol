// SPDX-License-Identifier: MIT
pragma solidity 0.8.24;

// Пустая заглушка, чтобы не тянуть fhenix.
// Основная логика у нас в AuraScoreBank.sol.
contract AuraScore {
    function noop() external pure returns (uint256) { return 0; }
}
